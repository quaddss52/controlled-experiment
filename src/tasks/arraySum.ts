/**
 * arraySum - Variation 4
 * Auto-generated for experiment
 */

export function arraySum(numbers: number[]): number {
  let sum = 0;
  for (const num of numbers) {
    sum += num;
  }
  return sum;
}
